---
- name: Converge
  hosts: localhost
  gather_facts: true
  tasks:
    - name: "Include ansible-role-xml-validation"
      ansible.builtin.include_role:
        name: "ansible-role-xml-validation"
